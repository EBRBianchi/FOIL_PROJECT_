<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <title>Login - evolua+</title>
    <style>
        body {
            display: flex;
            height: 100vh;
            margin: 0;
        }
        .left-side {
            background-color: #333;
            color: white;
            padding: 20px;
            width: 50%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
        .right-side {
            background-image: url('abstract-yellow-red-painting.jpg'); /* Substitua pelo caminho da imagem */
            background-size: cover;
            width: 50%;
        }
        input, button {
            margin: 10px 0;
            padding: 10px;
            width: 100%;
        }
    </style>
</head>
<body>
    <div class="left-side">
        <img src="logo.png" alt="evolua+ logo"> <!-- Substitua 'logo.png' pelo caminho do logo -->
        <h1>Login</h1>
        <form action="/login" method="post"> <!-- Substitua '/login' pelo link de envio do formulário -->
            <input type="email" id="email" name="email" placeholder="E-mail">
            <input type="password" id="senha" name="senha" placeholder="Senha">
            <button type="submit">Entrar</button>
        </form>
        <p>Necessito criar conta? <a href="/register">Crie uma conta</a>.</p> <!-- Substitua '/register' pelo link de registro -->
    </div>
    <div class="right-side">
        <!-- Imagem abstrata amarela e vermelha -->
    </div>
</body>
</html>
