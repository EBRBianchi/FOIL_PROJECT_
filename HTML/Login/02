<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <title>Página de Cadastro</title>
    <style>
        body {
            display: flex;
            height: 100vh;
            margin: 0;
        }
        .left-side {
            background-color: #333;
            color: white;
            padding: 20px;
            width: 50%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
        .right-side {
            background-image: url('abstract-face-painting.jpg'); /* Substitua pelo caminho da imagem */
            background-size: cover;
            width: 50%;
        }
        input, button {
            margin: 10px 0;
            padding: 10px;
            width: 100%;
        }
    </style>
</head>
<body>
    <div class="left-side">
        <img src="logo.png" alt="evolua! logo"> <!-- Substitua 'logo.png' pelo caminho do logo -->
        <h1>Cadastro</h1>
        <form action="/register" method="post"> <!-- Substitua '/register' pelo link de envio do formulário -->
            <input type="text" id="nome" name="nome" placeholder="Nome">
            <input type="text" id="sobrenome" name="sobrenome" placeholder="Sobrenome">
            <input type="text" id="cpf" name="cpf" placeholder="CPF">
            <input type="date" id="data_nascimento" name="data_nascimento" placeholder="Data de nascimento">
            <input type="email" id="email" name="email" placeholder="E-mail">
            <button type="submit">CADASTRAR</button>
        </form>
        <p>Já possui cadastro? <a href="/login">Entre</a>.</p> <!-- Substitua '/login' pelo link de login -->
    </div>
    <div class="right-side">
        <!-- Imagem abstrata com rosto -->
    </div>
</body>
</html>
